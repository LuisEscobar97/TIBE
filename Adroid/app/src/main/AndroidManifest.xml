<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sitol.tibe">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_tibe"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_tibe"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".turistas_citas.InfoCitas"
            android:noHistory="true"></activity>
        <activity
            android:name=".pirncipales.Splash"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pirncipales.Registro"
            android:noHistory="true" >

        </activity>
        <activity android:name=".adaptadorLista.InfoGuias"
            android:noHistory="true"/>
        <activity
            android:name=".pirncipales.NavegadorPrincipal"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBolWjEApvU-Co7gXufe9h1x58xKb0ftM8" />

        <activity
            android:name=".Detalles"
            android:label="@string/Detalle_guias" />
        <activity
            android:name=".turistas_AgendarCitas.Maps"
            android:label="@string/title_activity_maps"
            android:noHistory="true"/>
        <activity
            android:name=".turistas_AgendarCitas.AgendarCitas"
            android:label="@string/Agenda_citas"
            android:noHistory="true"/>
        <activity
            android:name=".CItasHist"
            android:label="@string/title_activity_citas_hist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".turistas_citas.EstadosDeCitas"
            android:label="@string/title_activity_estados_de_citas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".turistas_citas.ActivityPendientes" />
        <activity
            android:name=".pirncipales.Login"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>